const GalleryPage = () => {
  return (
    <main className="svm-page-content" style={{ padding: 'var(--section-padding-y) 0' }}>
      <div className="container">
        <h1 style={{ marginBottom: 'var(--space-4)' }}>Photo & Activity Gallery</h1>
        <p style={{ color: 'var(--color-text-secondary)' }}>Moments of learning, cultural celebrations, and student achievements.</p>
      </div>
    </main>
  );
};

export default GalleryPage;
